# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel


# typescript
*.tsbuildinfo
next-env.d.ts
local-*
.claude
*.log
dev.log
dev.out.log
test
prompt

server.log
# Skills directory
/skills/

# Firebase + deployment
# Service account key used by `bun run bootstrap` — full admin access. NEVER commit.
serviceAccount.json
# Firebase emulator/runtime debug logs.
firebase-debug.log
firestore-debug.log
ui-debug.log
# Any other *.local config (e.g. .env.local is already covered by .env* above).
*.local

# Uploaded files (may contain secrets — NEVER commit)
upload/
# Scaffold/debug artifacts not part of the portfolio app
.zscripts/
Caddyfile
examples/
mini-services/
download/
public/download/
# Debug screenshots
shot-*.png
# Firebase emulator persisted data (local only)
.firebase-data/
tool-results/
